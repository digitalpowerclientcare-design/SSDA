# Copy to .env and fill in. Vars prefixed PUBLIC_ are exposed to the browser.

# Microsoft Clarity project ID (from clarity.microsoft.com → Settings → Overview).
# Leave blank to keep Clarity off. When set, the tracking snippet loads site-wide.
PUBLIC_CLARITY_PROJECT_ID=
